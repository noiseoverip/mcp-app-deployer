apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-local-subnets
  namespace: {{ .Namespace }}
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
{{- range .AllowedSubnets }}
        - ipBlock:
            cidr: {{ . }}
{{- end }}
